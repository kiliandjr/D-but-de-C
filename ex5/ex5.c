#include <stdlib.h>
#include <stdio.h>
int main()
{

	double cinq=12;
        double quatre=7;
        double résulta= (quatre + cinq)/2;
        printf("(%.2f + %2.f)/2 = %2.f\n", cinq, quatre, résulta);
        exit(0);
}
